[package]
name = "image-subscriber"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
zenoh = { version = "1.1.0",  features = ["internal_config"]}
zenoh-config = { version = "1.1.0" }